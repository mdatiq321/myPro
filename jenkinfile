
pipeline {

    agent any
    triggers{
        cron(* * * * *)
        }


    stages {
        stage('Build and Execute Script') {
            steps {
                echo "Starting build stage.."
               
                sh "python3 test.py"
            }
        }
    }

  
    post {
        success {
            echo "Pipeline succeeded!"
        }
        failure {
            echo "Pipeline failed!"
        }
      
    }
}
